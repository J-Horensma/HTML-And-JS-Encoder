# HTML-And-JS-Encoder
**"HTML And JS Encoder"**, is an advanced multi-line input, HTML and JS multi-encoder, with the option to leave certain characters/continuous strings, unencoded.
It uses it's own encoding libraries, so not many requirements are neccessary, all but one of it's requirements, are pre-installed in python 3.
