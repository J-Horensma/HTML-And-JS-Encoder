# HTML-And-JS-Encoder
"HTML And JS Encoder", is an advanced multi-line input, HTML and JS multi-encoder, with the option to leave user-chosen characters/continuous strings, unencoded.
"HTML And JS Encoder" uses it's own encoding libraries, so not many requirements are neccessary.
All but one of this project's requirements, are pre-installed in python.
